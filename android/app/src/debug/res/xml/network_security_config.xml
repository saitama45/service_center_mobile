<?xml version="1.0" encoding="utf-8"?>
<!-- Debug builds only (see ../AndroidManifest.xml). Cleartext HTTP is
     permitted only to local dev-server addresses — everything else still
     requires TLS, same as production. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android emulator's alias for the host machine's loopback. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
